# Root .env for docker-compose (ONLYOFFICE Docs).
# Copy to `.env` and set a strong secret. It MUST match ONLYOFFICE_JWT_SECRET
# in backend/.env. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
ONLYOFFICE_JWT_SECRET=change-this-to-match-backend
