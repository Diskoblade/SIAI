"""Deck media generation: charts (Vega-Lite), diagrams (Mermaid), analytics
(DuckDB), and rich document parsing (Docling). Each renderer degrades
gracefully when its optional dependency is unavailable."""
