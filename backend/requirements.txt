# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Data / ORM ---
SQLAlchemy>=2.0.30
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0

# --- Security ---
PyJWT>=2.8.0            # Maintained JWT library (task permits an equivalent to python-jose)
argon2-cffi>=23.1.0    # Argon2id password hashing (no dependency on the removed stdlib `crypt`)

# --- Config ---
python-dotenv>=1.0.0

# --- RAG platform ---
python-multipart>=0.0.9   # multipart/form-data for document uploads
langgraph>=0.2            # agentic RAG orchestration graph
fluids==1.3.1             # validated fluid-dynamics calculations for the agent tool
# File ingestion formats exposed by the upload UI:
pypdf>=4.0                # PDF text extraction
python-docx>=1.1          # DOCX text + table extraction
openpyxl>=3.1             # XLSX extraction
# Vector database provider (used when VECTOR_STORE=qdrant):
qdrant-client>=1.11

# --- Deck media generation (charts / diagrams / analytics / parsing) ---
duckdb>=1.1               # in-process SQL analytics for deck data tables
vl-convert-python>=1.7    # render Vega-Lite chart specs to PNG (no browser)
# Diagrams: install mermaid-cli in backend/tools:  (cd tools && npm i @mermaid-js/mermaid-cli)
# Optional richer parser (heavy ML deps — used automatically when installed):
#   docling>=1.0           # structure-aware PDF/DOCX/PPTX parsing

# --- Tests ---
pytest>=8.0.0
httpx>=0.27.0
